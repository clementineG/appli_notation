{% extends '::base.html.twig' %}

{% block body -%}
    <h1>StudentClassCourse creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('studentClassCourse') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
