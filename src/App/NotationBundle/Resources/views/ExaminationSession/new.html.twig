{% extends '::base.html.twig' %}

{% block body -%}
    <h1>ExaminationSession creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('examinationSession') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
